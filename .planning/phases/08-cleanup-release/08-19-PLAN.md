---
phase: 08-cleanup-release
plan: 19
type: execute
wave: 8
depends_on: ["07-18"]
files_modified:
  - [DELETE] apps/desktop/
  - package.json (root)
  - turbo.json
  - README.md
autonomous: true

must_haves:
  truths:
    - Desktop app completely removed
    - No Electron dependencies remain
    - Root package.json updated
    - Build scripts work
    - No broken imports
    - Documentation updated
  artifacts:
    - path: package.json
      provides: Root workspace config
      contains: Updated scripts without desktop
    - path: README.md
      provides: Updated documentation
      contains: CLI-first instructions
  key_links:
    - from: package.json
      to: apps/cli, apps/server, packages/core
      via: workspaces list
---

<objective>
Delete the Electron desktop app and update workspace configuration.

Purpose: Complete the migration by removing old code and updating docs.
Output: Clean repository with only CLI and server, updated documentation.
</objective>

<execution_context>
@/Users/dylansteck/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/dylansteck/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/07-server-refactor/07-18-SUMMARY.md

Cleanup tasks:
- Delete apps/desktop/
- Remove from workspaces
- Update build scripts
- Update documentation
- Test everything still works
</context>

<tasks>

<task type="auto">
  <name>Delete Desktop App</name>
  <files>apps/desktop/ [DELETED]</files>
  <action>
Remove desktop app completely:

1. Delete apps/desktop/ directory:
   - rm -rf apps/desktop/

2. Verify deletion:
   - ls apps/ should show only cli/ and server/

3. Remove from git:
   - git rm -rf apps/desktop/
   - Or if already deleted: git add -A

4. Check for any references:
   - grep -r "apps/desktop" . --include="*.json" --include="*.md"
   - Should find no references

5. Note: This is destructive - ensure we have everything we need from desktop app:
   - Sync logic: moved to core ✓
   - IPC handlers: not needed in CLI ✓
   - UI components: replaced with TUI ✓
   - Config: moved to core ✓

Delete desktop app.
  </action>
  <verify>ls apps/ shows only cli and server</verify>
  <done>Desktop app deleted</done>
</task>

<task type="auto">
  <name>Update Root Configuration</name>
  <files>package.json, turbo.json</files>
  <action>
Update root workspace files:

1. Update package.json:
   - Remove desktop from workspaces if explicit
   - Update scripts:
     * Remove dev:desktop, build:desktop
     * Update dev to only run cli and server
     * Add cli command: "cli": "bun run --filter @backpack/cli"
   - Remove Electron-related devDependencies
   - Updated scripts:
     ```json
     {
       "scripts": {
         "dev": "turbo run dev --filter=@backpack/cli --filter=@backpack/server",
         "dev:cli": "turbo run dev --filter=@backpack/cli",
         "dev:server": "turbo run dev --filter=@backpack/server",
         "build": "turbo run build",
         "cli": "bun run --filter @backpack/cli",
         "test": "turbo run test"
       }
     }
     ```

2. Update turbo.json:
   - Remove desktop-related pipeline entries
   - Ensure cli and server are in pipeline
   - Update dependsOn arrays

3. Update lockfile:
   - rm -rf node_modules bun.lockb
   - bun install
   - This removes old Electron dependencies

4. Test workspace:
   - bun install completes
   - No errors about missing packages

Update configuration files.
  </action>
  <verify>cat package.json shows updated scripts</verify>
  <done>Root configuration updated</done>
</task>

<task type="auto">
  <name>Test Build and Scripts</name>
  <files>package.json</files>
  <action>
Verify everything works after cleanup:

1. Clean install:
   - rm -rf node_modules
   - bun install
   - Verify no errors

2. Build all packages:
   - bun run build
   - Should build: core, api, cli, server
   - No errors

3. Test CLI:
   - bun run cli -- --help
   - Should show CLI help

4. Test type checking:
   - bun run check-types (if available)
   - Or: cd packages/core && tsc --noEmit
   - Should pass

5. Verify no broken imports:
   - grep -r "@backpack/desktop" . --include="*.ts"
   - Should find nothing

6. Test core package:
   - cd packages/core && bun run build
   - Should compile

7. Check for orphaned files:
   - Any .desktop references?
   - Any electron references?

Everything should build and run.
  </action>
  <verify>bun run build completes without errors</verify>
  <done>Build and scripts verified</done>
</task>

<task type="auto">
  <name>Update Documentation</name>
  <files>README.md, AGENTS.md</files>
  <action>
Update documentation for CLI-first:

1. Update README.md:
   - Update title: "Backpack - Personal Operating System (CLI)"
   - Rewrite introduction to focus on CLI
   - Update architecture section:
     * Remove Electron references
     * Add CLI architecture
     * Document TUI mode
   - Update installation:
     * npm install -g @backpack/cli
     * Or build from source
   - Update usage:
     * backpack timeline
     * backpack tui
     * backpack sync
     * backpack search
   - Add TUI screenshots (ASCII art or descriptions)
   - Update development:
     * pnpm dev:cli
     * pnpm dev:server
   - Remove Electron-specific sections

2. Update AGENTS.md:
   - Update project overview
   - Update architecture (no Electron)
   - Update monorepo structure
   - Add CLI section
   - Add TUI section
   - Remove Electron performance guide references
   - Update common tasks for CLI

3. Create CLI.md:
   - Document all commands
   - Show examples
   - Document TUI usage
   - Document configuration
   - Document daemon mode

4. Update or create:
   - CHANGELOG.md for v2.0
   - Mention migration from Electron to CLI

Documentation reflects new architecture.
  </action>
  <verify>cat README.md | head -50 shows CLI focus</verify>
  <done>Documentation updated for CLI</done>
</task>

</tasks>

<verification>
- [ ] apps/desktop/ deleted
- [ ] No desktop references in code
- [ ] package.json scripts updated
- [ ] turbo.json updated
- [ ] bun install works
- [ ] bun run build works
- [ ] bun run cli -- --help works
- [ ] README.md updated
- [ ] AGENTS.md updated
- [ ] No build errors
</verification>

<success_criteria>
- Desktop app fully removed
- Workspace builds successfully
- CLI runs
- No references to deleted code
- Documentation accurate
- Repository is clean
</success_criteria>

<output>
After completion, create `.planning/phases/08-cleanup-release/08-19-SUMMARY.md` with:
- What was deleted
- Build verification results
- Documentation updates
- Next steps (Plan 20: release)
</output>
